[Unit]
Description=Periodically rebuild the Blueberry package repo from git

[Timer]
# Rebuild a few minutes after boot, then hourly. Building is idempotent —
# unchanged recipes just recompile and re-publish the same packages.
OnBootSec=5min
OnUnitActiveSec=1h
Persistent=true

[Install]
WantedBy=timers.target
